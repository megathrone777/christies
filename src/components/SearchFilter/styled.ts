import styled from "styled-components";
import { fonts, rem } from "../App/styled";

export const Wrapper = styled.div`
  padding-right: 20px;
`;

export const Title = styled.h2`
  padding-left: 20px;
  font: normal ${rem(14)} ${fonts.fontMedium};
  margin-bottom: 15px;
  line-height: ${rem(40)};
`;

export const Layout = styled.div``;
